# CLAUDE.md — TCG Marketplace (เว็บซื้อขายการ์ดสะสม)

> ไฟล์นี้คือ context หลักของโปรเจกต์ อ่านก่อนเริ่มทำงานทุกครั้ง
> เอกสารออกแบบฉบับเต็ม (flow + diagram + บันทึกการตัดสินใจ) อยู่ที่ `tcg-marketplace-spec.html` — เปิดอ่านประกอบ

---

## 1. โปรเจกต์นี้คืออะไร

เว็บช่วยซื้อขายการ์ดสะสม (เริ่มที่ Pokemon) แก้ปัญหาความยุ่งยากของการขายบน Facebook
โดยเฉพาะ **การลงขายการ์ดทีละใบที่กินแรงมาก**

**Hero feature (จุดขายหลัก):** ผู้ขายถ่ายรูปการ์ดหลายใบในรูปเดียว → ระบบตัดกรอบแยกเป็นรายใบ →
ผู้ซื้อแตะการ์ดแต่ละใบในรูปเพื่อหยิบเข้าตะกร้าได้ทันที (ทั้ง FB และคู่แข่งยังไม่มี)

---

## 2. Positioning (สำคัญที่สุด — ห้ามหลุด)

- เฟสแรกเป็น **"เครื่องมือช่วยขาย" ไม่ใช่ "platform กลางการซื้อขาย"** — แข่งกับ Facebook ที่ความง่าย ไม่ใช่แข่ง marketplace เต็มตัว
- **เงินผู้ซื้อโอนตรงเข้าบัญชีผู้ขาย ไม่ผ่านระบบเรา** → ไม่มี escrow ไม่ถือเงินใคร (เลี่ยงภาระใบอนุญาต e-money)
- เว็บทำหน้าที่แค่ **ตรวจสลิป** ให้ แล้วตัด stock — ไม่รับประกันการโกง ต้องมี disclaimer ชัดในหน้าเว็บ
- จุดขาย = **UX การลงขาย/ซื้อ** ไม่ใช่ความปลอดภัย

---

## 3. Scope เฟสแรก (MVP) — สร้างแค่นี้ก่อน

เป้าหมายเดียว: พิสูจน์ว่า **"ผู้ขายลงของง่ายกว่า FB + ผู้ซื้อจ่ายจบในเว็บ"** มีคนใช้จริงไหม
อะไรที่ไม่จำเป็นต่อ loop นี้ → เลื่อนไปเฟสต่อไป (ดูข้อ 4)

### ฝั่งผู้ขาย
- **Onboarding:** สมัคร (Google + email/password) → ตั้งโปรไฟล์ร้าน (ชื่อร้าน + URL) → **ผูกบัญชีรับเงิน** (บังคับ ตั้งแต่สมัคร)
- **ลงขาย batch:** ถ่ายรูปหมู่ → ระบบตัดกรอบ → ผู้ขายตรวจ/แก้กรอบ → ตั้งชื่อ/ราคา/สภาพ (ระบบ default ให้) → ขึ้นขาย
- **ลงขายเดี่ยว:** ใช้ flow เดียวกับ batch (ต่างแค่ไม่มีขั้นตัดกรอบหลายใบ)
- **กด "ขายแล้ว"** (ถ้าขายที่อื่น) → ระบบเกรย์ใบทันที
- **จัดการสินค้า:** แก้ราคา/จำนวน, ลบ (เฉพาะใบยังไม่ขาย), เติมของใหม่ (reuse batch)
- **Dashboard ออเดอร์:** แยกสถานะ รอส่ง / ส่งแล้ว / จบ + ใส่เลขพัสดุ (พิมพ์เอง)
- **สร้างโพสต์ → copy link** + หน้ารวมร้าน (1 link เห็นของทั้งร้าน) + OG preview

### ฝั่งผู้ซื้อ
- **เปิดดูแบบ guest** (ไม่ต้องสมัคร) → กดการ์ดทีละใบในรูป → ซูมดู → ใส่ตะกร้า
- **Checkout (guest):** แนบสลิปในเว็บ → **ตรวจ QR อัตโนมัติ (slip2go API)** เทียบบัญชีร้าน + ยอด → ผ่าน → ตัด stock ทันที → กรอก email → ได้ใบเสร็จ + link เช็คสถานะ
- สลิปไม่ผ่าน → แจ้งผู้ซื้อ ไม่ตัด stock

### ระบบหลังบ้าน
- **ID ต่อการ์ดทุกใบ + รูป canonical ผูกถาวร** (ของที่กด = ของที่จ่าย = ของที่ได้)
- **3 สถานะล็อก:** ว่าง → reserved (ล็อกตั้งแต่เริ่ม checkout + freeze ราคา) → sold (ล็อกถาวร)
- **Timer 10 นาที** นับจาก "เวลาแนบสลิป" ไม่นับเวลาตรวจ — หมดเวลา = ปลดล็อกกลับ stock
- แจ้งเตือนราคาเปลี่ยนตอน checkout + ปุ่มยกเลิกออเดอร์ (มีตลอด)
- แจ้งเตือน in-app 🔔 + email (ออเดอร์ใหม่ / อัปเดตสถานะ)
- **เก็บราคา ณ จุดปิดดีล** ลงฐานข้อมูล (สะสมไว้ทำ price index ภายหลัง — เฟสแรกยังไม่ต้องแสดงผล)

---

## 4. เฟสต่อไป (อย่าเพิ่งทำ)

Vault (collection tracker) · Buy order + bookmark การ์ด + waitlist · โปรโมชั่น (ของแถม/แลกซื้อ) ·
หน้าค้นหาเต็ม + card catalog (TCGdex self-host) + แสดง price index · Tier/subscription (เฟสแรกฟรีหมด) ·
web push · OCR เลขพัสดุ · KYC · ตะกร้ารวมร้าน · consignment · ประมูล · ประมูลพื้นที่ร้านของเสริม

---

## 5. กฎ/logic สำคัญ (business rules — ห้ามทำผิด)

1. **เงินไม่ผ่านระบบ** — ห้ามทำฟีเจอร์ถือเงิน/escrow ในเฟสแรกเด็ดขาด
2. **ตัด stock ตอนตรวจสลิปผ่านเท่านั้น** ไม่ใช่ตอนใส่ตะกร้า
3. **Lock การ์ดตั้งแต่เริ่ม checkout** (ก่อนแนบสลิป) ไม่ใช่ตอนตรวจเสร็จ — กันเคส "จ่ายแล้วของหาย"
4. **ราคา freeze ตอน reserved** (snapshot ตอนกด checkout) — ผู้ขายแก้ไม่ได้ระหว่างมีคนซื้อ
5. **ใบ sold = read-only ถาวร** (ราคา/ข้อมูล lock เพราะเป็น record ของดีลจริง)
6. **ลดจำนวนได้ แต่ห้ามต่ำกว่าที่ขายไปแล้ว** (ลง 5 ขายไป 4 → ลดได้ต่ำสุด 4)
7. **ผู้ซื้อไม่ต้องสมัครจนกว่าจะจ่าย** — เก็บแค่ email ตอน checkout, upgrade เป็นสมาชิกทีหลังได้
8. **ผู้ขายบังคับผูกบัญชีตั้งแต่สมัคร** (ระบบตรวจสลิปต้องใช้เทียบ) — บัญชีใส่ของใครก็ได้ ไม่เทียบชื่อ เช็คแค่เลขมีจริง
9. **slip2go/EasySlip = abstraction layer** อย่าผูกติด provider เดียวในโค้ด (เผื่อสลับ) + กัน Token หมด (auto top-up + เตือน)
10. **ราคากลาง = ราคา ณ จุดปิดดีลจริงเท่านั้น** (bid ลอยๆ ไม่นับ) + ใช้ median ตัด outlier กันปั่น

---

## 6. ทิศทาง Tech (ข้อเสนอ — ปรับได้)

- **Frontend/Framework:** Next.js (หรือที่ถนัด) — ต้องรองรับ mobile-first (ลูกค้าส่วนใหญ่ใช้มือถือ)
- **Auth:** ใช้ library สำเร็จรูป (Supabase Auth / Clerk / Auth.js) — Google + email/password, เพิ่ม provider อื่นทีหลังง่าย
- **Database:** managed (เช่น Supabase/Postgres) — เก็บข้อมูลบัญชีแบบ **เข้ารหัส (at rest)**
- **Storage รูป:** object storage ราคาประหยัด (เช่น Cloudflare R2)
- **ตรวจสลิป:** slip2go API (API Connect) — ออกแบบเป็น interface สลับ provider ได้
- **ตัดกรอบการ์ด (CV):** ตัดกรอบ/นับจำนวนใบเท่านั้น (bounding box) — **ไม่ต้องระบุว่าเป็นการ์ดอะไร** (ผู้ขายใส่ชื่อเอง/เว้นว่างได้)
- **PDPA:** มี privacy policy + ขอความยินยอม ตั้งแต่ launch (กฎหมายไทยบังคับ)
- ใช้ของ **free tier** ให้มากที่สุด — ต้นทุนเป้า ~฿500–800/เดือน ช่วงแรก

---

## 7. Test cases (ให้เขียน test ครบทุกฟีเจอร์)

**ทำไมต้องเน้น test:** ระบบนี้แตะ "เงินจริง + stock ชิ้นเดียว (ขายแล้วหมดเลย)" — บั๊กแม้นิดเดียว
แปลว่า **ลูกค้าจ่ายเงินแล้วไม่ได้ของ / การ์ดถูกขายซ้ำ 2 คน / stock เพี้ยน** = เสียเงินจริงและเสียความเชื่อใจถาวร
ไม่ใช่แค่ UI เพี้ยนแล้ว refresh ได้ จึงต้องมี test ครอบคลุม โดยเฉพาะ logic การล็อก/จ่ายเงิน/stock

ให้เขียน test ครบทั้ง 3 ระดับ:

### 7.1 Unit test — logic ย่อยที่ตัดสินใจถูก/ผิดได้
- คำนวณ "ลดจำนวนได้ต่ำสุด = จำนวนที่ขายไปแล้ว" (ลอง boundary: ลดเท่ากับ, ต่ำกว่า, สูงกว่า)
- ตรวจสลิป: parse ผลจาก API → เทียบบัญชีร้าน + ยอด → ผ่าน/ไม่ผ่าน (mock API response ทั้งกรณีจริง/ปลอม/ซ้ำ/ผิดบัญชี)
- price snapshot: ราคาที่ผูกกับ order = ราคา ณ วินาที checkout (ไม่ใช่ราคาที่ผู้ขายแก้ทีหลัง)
- timer: คำนวณหมดอายุจาก "เวลาแนบสลิป"

### 7.2 Integration / flow test — เส้นทางจริงทั้ง flow
- **ขาย batch:** ถ่าย → ตัดกรอบ N ใบ → ทุกใบได้ ID → ขึ้นขายครบ
- **ซื้อ guest:** เปิดดู (ไม่ login) → ใส่ตะกร้า → checkout → แนบสลิป → ตรวจผ่าน → ตัด stock → ได้ email + link
- **สลิปไม่ผ่าน:** ต้องไม่ตัด stock + แจ้งผู้ซื้อ
- **timer หมด:** checkout แล้วไม่แนบสลิปใน 10 นาที → ปลดล็อก กลับเป็น "ว่าง"
- **onboarding:** บัญชีเลขผิด → verify ไม่ผ่าน → แก้ได้
- **จัดการสินค้า:** แก้ราคาใบยังไม่ขาย → หน้าโพสต์อัปเดตตาม / แก้ใบ sold → ต้องถูกบล็อก

### 7.3 Edge case / concurrency test — จุดที่พังแล้วเสียเงิน (สำคัญสุด)
- **2 คนกดซื้อการ์ดใบเดียวกันพร้อมกัน** → ต้องมีคนเดียวได้ (คนแรกที่ checkout ล็อกได้) อีกคนเห็น "ถูกจองแล้ว/ขายแล้ว"
- **ผู้ขายพยายามลด/ลบการ์ดที่กำลังถูก checkout (reserved)** → ต้องถูกบล็อก
- **ผู้ขายแก้ราคาเสี้ยววินาทีเดียวกับที่ผู้ซื้อกด checkout** → ผู้ซื้อต้องได้ราคา snapshot ตอนกด ไม่ใช่ราคาใหม่
- **แนบสลิปนาทีที่ 9:55 ระบบตรวจเสร็จนาทีที่ 10:30** → ต้องไม่หลุด (นับเวลาแนบ ไม่นับเวลาตรวจ)
- **สลิปซ้ำ / สลิปเก่า** ที่เคยใช้แล้ว → ต้องถูกปฏิเสธ
- **Token slip2go หมดกลางทาง** → ระบบต้องไม่ทำให้ออเดอร์ค้างเงียบ (มี fallback/แจ้งเตือน)
- **ลบการ์ดที่มีคนถือในตะกร้า (ยังไม่ checkout)** → ตะกร้าคนนั้นต้องเด้งแจ้ง ไม่หายเงียบ

**หลักการเขียน test:** ทุกครั้งที่มี logic เกี่ยวกับ **เงิน / stock / การล็อก** ต้องมี test คุม
โดยเฉพาะ concurrency (คนหลายคนทำพร้อมกัน) เพราะเป็นจุดที่เกิดบ่อยในตลาดจริงและพังเงียบที่สุด

---

## 8. วิธีทำงานกับเจ้าของโปรเจกต์

- คุยทีละเรื่อง ไม่เทรวดเดียว
- อยากได้ feedback ตรงๆ รวมถึงช่องโหว่/ความเสี่ยง — บอกตามจริงแม้ขัดใจ
- เริ่มสร้างที่ **hero feature: batch upload** ก่อนเสมอ อย่าไล่ทำให้ครบทุกฟีเจอร์
- ภาษาหลัก: ไทย (ศัพท์เทคนิคใช้อังกฤษได้)
